# usage:
#   ./sincos > data.log
#   gnuplot -persist sincos.gplot
#

datafile = "data.log"

set terminal x11;
# set terminal ps;

set title "output of sincos"
set grid

plot datafile using 1:2 title "y1", \
     datafile using 1:5 title "zero crossings (on x-axis)" with points 20

